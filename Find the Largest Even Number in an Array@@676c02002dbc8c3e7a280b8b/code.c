#include <stdio.h>
int main(){
    int n,k;
    scanf("%d %d", &n, &k);
    for(int i=0;i<n;i++{
        scanf("%d",arr[i]);
    })

    int largest_elment=0;
    for(int i=0;i<n;i++){
        if(arr[n]%2==arr[i]){
            largest_elment++;
            printf("%d",largest_elment);
        }
    }
}